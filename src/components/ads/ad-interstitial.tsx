'use client'

/**
 * AdInterstitial — DESABILITADO.
 * Não há widget interstitial criado no painel Adskeeper.
 * Componente vazio que não renderiza nada.
 */
export function AdInterstitial() {
  return null
}